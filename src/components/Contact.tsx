import React from "react";
import { BackToTop } from "./BackToTop";

type Props = {}

export const Contact = (props: Props) => {
  return (
    <div>Contact
      {/* <BackToTop /> */}
    </div>
  );
};